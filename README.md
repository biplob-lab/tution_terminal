# Tuition Terminal — Tutor Dashboard Platform

A terminal-inspired tutoring portal dashboard built using HTML, CSS, Vanilla JS, and Python (Flask).

## Project Setup
1. Install dependencies:
   ```bash
   pip install flask